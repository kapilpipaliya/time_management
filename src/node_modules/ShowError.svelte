<script>
import { onMount } from "svelte";
export let er = "";

const showErr = (msg) => {
  er = msg;
  setTimeout(() => er= "", 4000);
};
onMount(()=>{
  showErr(er);
})

</script>
<template lang="pug">
+if('er')
  span.accent-block.warning.text-center {er}
  br
</template>

