<script  lang="typescript">
    import * as A from 'index.ts'

    export let row;
    export let pathp=undefined;
    export let paths='';
    $: editpath = A.uPath() + '/' + (pathp ? pathp + '/' : '') + row.getUid() + paths;
</script>
<td>
    <a class='button outline info' href={editpath} on:click> Edit</a>
</td>
