//- pug
ShowError(er='{$er}')
Add(on:click='{c.onNewClick}')
Table.table.table-border.row-border(data='{$data}' filters='{filters}' pageSize='{pageSize}' currentPage='{currentPage}' selectedClass='secondary')
  thead(slot='head')
    th Edit
    th Name
    th Is Default
    th Is Closed
    th Color
    th Default Done Ratio
    th Is Readonly
  tbody(slot='body' let:displayData='{displayData}')
    +each('displayData as row')
      Tr({row})
        td
          a(href='/admin/users/' + '{row.getUid()}') Edit
        td {row.getUid()}
        td {row.getName()}
        td {row.getIsDefault()}
        td {row.getIsClosed()}
        td {row.getColor()}
        td {row.getDefaultDoneRatio()}
        td {row.getIsReadonly()}
Pagination({totalPages} bind:currentPage='{currentPage}')

//- pug_end
